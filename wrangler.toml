name = "terminal-resume"
main = "src/index.js"
compatibility_date = "2024-01-01"

# 开发环境配置
[dev]
port = 8787
local_protocol = "http"

# 生产环境变量 (可选)
[vars]
ENVIRONMENT = "production"

# 自定义域名路由 (部署后在 Cloudflare Dashboard 配置)
routes = [{ pattern = "pdjjq.org/*", zone_name = "pdjjq.org" }]
